#include "stdio.h"

int main()
{
    int i，miniNum;
    //首先有个数组
    int nums[5] = {5, 3, 2, 2, 5};
    //找到最小值需要遍历数组
    //假设一个最小值
    miniNum = nums[0];
    for (i = 0; i < 5; i++)
    {
        if (/* condition */)
        {
            /* code */
        }
    }
}